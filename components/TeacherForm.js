import { useState } from 'react';
const fields=[['full_name','Full Name'],['email','Email','email'],['phone','Phone'],['subject','Subject'],['qualification','Qualification'],['joining_date','Joining Date','date'],['monthly_salary','Monthly Salary','number'],['working_hours','Working Hours','number']];
export default function TeacherForm({initial={},onSubmit,submitText='Save Teacher'}){const [form,setForm]=useState({status:'active',working_hours:'8',...initial});const [busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);try{await onSubmit(form)}finally{setBusy(false)}}return <form onSubmit={submit} className="grid gap-4 sm:grid-cols-2">{fields.map(([k,l,t='text'])=><label key={k}><span className="label">{l}</span><input className="field" required type={t} min={k==='monthly_salary'||k==='working_hours'?'0':undefined} value={form[k]||''} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<label><span className="label">Status</span><select className="field" value={form.status} onChange={e=>setForm({...form,status:e.target.value})}><option value="active">Active</option><option value="inactive">Inactive</option></select></label><div className="sm:col-span-2"><button className="btn-primary" disabled={busy}>{busy?'Saving…':submitText}</button></div></form>}
